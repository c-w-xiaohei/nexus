{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "types": ["chrome"] // Add this line to tell TypeScript to include Chrome global types
  },
  "include": ["src", "vite.config.ts"],
  "exclude": ["node_modules", "dist"]
}
